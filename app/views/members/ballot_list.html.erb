<% content_for :heading, 'Ballot List' %>

<p>
  Use this page to cross-check members at AGMs, SGMs, ballots and elections.
  Any Member of the society may attend these meetings but only Ordinary Members with unexpired or lifetime membership may vote.
  This page lists all such Ordinary Members.
  Please use ctrl+F (cmd+F on Mac) to search this page from your web browser.
</p>

<table class="table table-condensed table-bordered table-hover">
  <thead>
    <%= content_tag :tr do %>
      <%= content_tag(:th, 'Full Name') %>
      <%= content_tag(:th, 'CRSid') %>
      <%= content_tag(:th, 'Graduation Year') %>
      <%= content_tag(:th, 'Expiry Date') %>
    <% end %>
  </thead>
  <tbody>
    <% @members.each do |member| %>
      <%= content_tag :tr do %>
        <%= content_tag(:td, link_to(member.full_name, member)) %>
        <%= content_tag(:td, member.crsid) %>
        <%= content_tag(:td, member.graduation_year) %>
        <%= content_tag(:td, expiry_text(member)) %>
      <% end %>
    <% end %>
  </tbody>
</table>
